@model OnshoreSDAttendanceTrackerNet.Models.UserViewModel

@{
    ViewBag.Title = "UpdateUserInfo";
}

<h2>UpdateUserInfo</h2>


@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <h4>UserViewModel</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        <div class="form-group">
            @Html.LabelFor(model => model.ErrorMessage, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.ErrorMessage, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.ErrorMessage, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

@*<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}

@using BasketballScoreBook.Models
@model BasketballScoreBook.Models.UserViewModel

@{
    ViewBag.Title = "UpdateUser";
}

<h2 class="UserHeader">Update User</h2>

@using (Html.BeginForm("UpdateUser", "User", FormMethod.Post))
{
    @Html.ValidationSummary(false, "", new { @class = "text-danger" })

    <div class="UserInfoBox">
        <div class="LabelDisplayTextBoxPos">
            <div class="LabelSpacing">
                @Html.HiddenFor(m => m.SingleUser.UserID)
            </div>
        </div>

        <div class="LabelTextBoxSet">
            <div class="LabelSpacing">
                @Html.LabelFor(m => m.SingleUser.UserName)
                @Html.TextBoxFor(m => m.SingleUser.UserName, new { @class = "form-control", placeholder = "m.SingleUser.UserName", @readonly = "readonly" })
            </div>
        </div>

        <div class="LabelTextBoxSet">
            <div class="LabelSpacing">
                @Html.LabelFor(m => m.SingleUser.RoleID)
                @Html.TextBoxFor(m => m.SingleUser.RoleID, new { @class = "form-control", placeholder = "m.SingleUser.RoleID", @type = "number" })
            </div>
            @*<div class="">
                    @Html.DropDownListFor(m => m.SingleUser.RoleID, new List<SelectListItem>
                        {
                        new SelectListItem {Text=" -- ", },
                        new SelectListItem {Text="1" },
                        new SelectListItem {Text="2" },
                        new SelectListItem {Text="3" } })

                </div>*@
        </div>

    </div>
    <div class="UserSubmitBox">
        <input type="submit" value="Submit" />
    </div>
}*@

